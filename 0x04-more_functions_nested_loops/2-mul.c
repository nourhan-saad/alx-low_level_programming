#include "main.h"
#include <stdio.h>

/**
 * mul - multublay the two intigir output
 *
 * @a: first input
 *@b: secound input
 *
 * Return: the multublay of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
